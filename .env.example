# RTMP app/name you will push into
STREAM_APP=live
STREAM_NAME=test

# Optional: single output (for Paso 1 demo). Example: Twitch RTMP base (no key here).
# Later we'll split each platform cleanly into its own variable set.
# For now keep it empty; we’ll validate the local ingest first.
SINGLE_OUTPUT_URL=

# Pod/network
RTMP_PORT=1935
